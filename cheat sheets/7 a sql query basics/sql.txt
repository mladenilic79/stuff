
SELECT
	*
	DISTINCT/UNIQUE
	TOP/LIMIT
	FUNCTIONS///ROUND/FLOOR/ABS/POW/COALESCE/NOW/CURRENT_TIMESTAMP
		CURDATE/CURTIME/LENGTH/CONCAT/LOWER/UPPER/LTRIM/REPLACE/
		LEFT/SUBSTRING/CONVERT/CAST/ISNULL/NULLIF
	AGREGATS///COUNT/SUM/AVG/MAX/MIN
FROM
	INNER JOIN
		ON
	LEFT JOIN
		ON
	RIGHT JOIN
		ON
	FULL JOIN
		ON
	FULL OUTER JOIN
		ON
	CROSS JOIN
		ON
WHERE
	NOT
	LIKE
	IN
	BETWEEN AND
	IS NULL/IS NOT NULL
	AND/OR
GROUP BY
HAVING
	AGREGATS///COUNT/SUM/AVG/MAX/MIN
ORDER BY
	ASC/DESC
UNION/EXCEPT/INTERSECT
SUBQUERIE
